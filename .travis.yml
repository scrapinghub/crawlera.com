language: ruby

rvm:
  - 2.1

script:
  - bundle exec jekyll build

deploy:
  provider: s3
  access_key_id: 'to be filled'
  secret_access_key: 'to be filled'
  bucket: 'to be filled'
  skip_cleanup: true
  local_dir: _site
  acl: public_read
  on:
    branch: master
